Hi {{ customer_first_name|default('there') }},

{{ store_name }} did not accept return #{{ return_id }}.

Reason given: {{ rejection_reason }}

Settle this directly with the store, as set out in the return agreement you accepted. We record the decision and the reason only.

{{ frontend_base_url }}/account/returns/{{ return_id }}
