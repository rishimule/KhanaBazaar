Hi {{ customer_first_name|default('there') }},

Return #{{ return_id }} for order #{{ order_id }} expired.

It was not completed in time, so the items are free to return again. If the return window on this order is still open you can start a new one.

{{ frontend_base_url }}/account/returns/{{ return_id }}
